#
# Apache Atlas configuration for aegir — HMS-free, Apache AGE backend on the
# SHARED aegir_hx graph. The two lines that make it shared:
#   atlas.age.jdbc.url  -> the aegir DB on :5555 (same Postgres the pipeline uses)
#   atlas.age.graph.name -> aegir_hx (the same graph the projector + OL variant write)
# Atlas owns entity vertices there; our provenance (RE_GROUNDS_TO, OL runs) rides
# the same graph as app-layer adapters referencing those entities (no duplication).
#

#########  Graph Database Configs  #########
atlas.graphdb.backend=org.apache.atlas.repository.graphdb.age.AtlasAgeGraphDatabase
atlas.age.jdbc.url=jdbc:postgresql://localhost:${env:PGPORT}/aegir
atlas.age.jdbc.user=${sys:user.name}
atlas.age.jdbc.password=
atlas.age.graph.name=aegir_hx
atlas.age.pool.size=50

atlas.graph.storage.backend=age
atlas.graph.index.search.backend=age
atlas.search.gremlin.enable=false
atlas.EntityAuditRepository.impl=org.apache.atlas.repository.audit.NoopEntityAuditRepository

#########  Notification Configs (Kafka disabled — no embedded broker) #########
atlas.notification.embedded=false
atlas.notification.consumer.disabled=true
atlas.kafka.data=${sys:atlas.home}/data/kafka
atlas.kafka.zookeeper.connect=localhost:9026
atlas.kafka.bootstrap.servers=localhost:9027
atlas.kafka.zookeeper.session.timeout.ms=400
atlas.kafka.zookeeper.connection.timeout.ms=200
atlas.kafka.zookeeper.sync.time.ms=20
atlas.kafka.auto.commit.interval.ms=1000
atlas.kafka.hook.group.id=atlas
atlas.kafka.enable.auto.commit=false
atlas.kafka.auto.offset.reset=earliest
atlas.kafka.session.timeout.ms=30000
atlas.kafka.offsets.topic.replication.factor=1
atlas.kafka.poll.timeout.ms=1000
atlas.kafka.max.block.ms=1000
atlas.kafka.request.timeout.ms=1000
atlas.kafka.delivery.timeout.ms=2000
atlas.notification.create.topics=true
atlas.notification.replicas=1
atlas.notification.topics=ATLAS_HOOK,ATLAS_ENTITIES
atlas.notification.log.failed.messages=true
atlas.notification.consumer.retry.interval=500
atlas.notification.hook.retry.interval=1000

#########  Server Properties  #########
atlas.server.http.port=21000
atlas.rest.address=http://localhost:21000
atlas.enableTLS=false
atlas.server.ha.enabled=false

#########  Authentication / Authorization  #########
atlas.authentication.method.kerberos=false
atlas.authentication.method.file=true
atlas.authentication.method.ldap.type=none
atlas.authentication.method.file.filename=${sys:atlas.home}/conf/users-credentials.properties
atlas.authorizer.impl=simple
atlas.authorizer.simple.authz.policy.file=atlas-simple-authz-policy.json

#########  CSRF  #########
atlas.rest-csrf.enabled=true
atlas.rest-csrf.browser-useragents-regex=^Mozilla.*,^Opera.*,^Chrome.*
atlas.rest-csrf.methods-to-ignore=GET,OPTIONS,HEAD,TRACE
atlas.rest-csrf.custom-header=X-XSRF-HEADER

############ Metrics ############
atlas.metric.query.cache.ttlInSecs=900
