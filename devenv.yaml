# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling

# If you're using non-OSS software, you can set allowUnfree to true.
allowUnfree: true  # CUDA (cudaPackages_12_8) for the fused RWKV-7 training-kernel compile

# No permittedInsecurePackages: the devenv MinIO (marked insecure upstream) was
# retired 2026-09-05 — the object store is Signals' RustFS on 127.0.0.1:9010.

# If you have more than one devenv you can merge them
#imports:
# - ./backend
